---
platform: linux

image_resource:
  type: docker-image
  source: { repository: goreleaser/goreleaser }

inputs:
  - name: source

params:
  GITHUB_TOKEN:

outputs:
  - name: dist

run:
  path: bash
  args:
  - -c
  - |
    cd source && goreleaser release
    cp cp -R source/dist/. dist/
